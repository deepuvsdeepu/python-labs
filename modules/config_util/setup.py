from setuptools import setup
setup(name='config_util',
      version='0.0.5',
      description='A simple plugin to load config and',
      url='http://example.com/',
      author='Pradeep G',
      author_email='pradeep@example.com',
      license='MIT',
      packages=['config_util', ''],
      zip_safe=False)